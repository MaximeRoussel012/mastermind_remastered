cmmandes sur terminale pour (re)lancer le jeu:
mkdir build 
cd build
cmake ..
cmake --build .

Lancez ensuite dans le main.exe du dossier build afin de jouer.

On rappel que le jeu contient 4 chiffres compris entre 0 et 5, par exemple:
0 0 1 5